# SRZ80 Issue Tracker

This repository serves as the issue tracker for SRZ80.

## Getting started
Head over to [here](https://github.com/LovelyA72/srz80-plugins) and grab the latest plugin bundle release. Make sure `plugins/` and `tools/` are in the same directory as `srz80_gui.exe`.
